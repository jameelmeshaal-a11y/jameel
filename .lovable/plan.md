

# خطة التنفيذ — 3 تحسينات على واجهة جدول BoQ

## ملخص
تطبيق 3 تحسينات على `src/components/BoQTable.tsx`: شارة 🔒 للبنود اليدوية، فلترة سريعة جديدة (الكل/يدوي/غير يدوي/pending)، وإخفاء أعمدة التحليل افتراضياً.

## التفاصيل

### 1. شارة البنود اليدوية 🔒
- الشارة **موجودة فعلاً** في السطر 856-858 تحت عمود الوصف
- سيتم تحسينها لتكون أكثر وضوحاً: حجم أكبر قليلاً + لون أقوى + نقلها لتظهر بجانب رقم البند أيضاً

### 2. فلترة سريعة جديدة
- إضافة 4 أزرار فلترة جديدة في شريط الفلترة الموجود (سطر 764-802):
  - **الكل** — يمسح كل الفلاتر
  - **يدوي معتمد 🔒** — يعرض فقط `override_type === 'manual'`
  - **غير يدوي** — يعرض فقط `override_type !== 'manual'` (قابل للمراجعة)
  - **pending** — يعرض فقط `status === 'pending'`
- تعديل `filteredItems` useMemo لدعم الفلاتر الثلاثة الجديدة
- الفلاتر الجديدة تعمل بمنطق AND مع الفلاتر الموجودة

### 3. إخفاء أعمدة التحليل افتراضياً
- إضافة state: `const [showBreakdown, setShowBreakdown] = useState(false)`
- إخفاء 6 أعمدة (المواد، العمالة، المعدات، اللوجستيات، المخاطر، الربح) من `<thead>` و `<tbody>`
- إضافة زر toggle صغير بجانب رأس الجدول لإظهار/إخفاء الأعمدة
- عند الضغط على أيقونة العين (Eye) لأي بند، يفتح PriceBreakdownModal كالمعتاد (لا تغيير)

## الملفات المتأثرة

| الملف | التغيير |
|---|---|
| `src/components/BoQTable.tsx` | فلاتر جديدة + toggle أعمدة التحليل + تحسين شارة 🔒 |

