

# إصلاح مطابقات خاطئة — سجل "حوائط القص الخرسانية"

## المشكلة
سجل المكتبة "حوائط القص الخرسانية" (id: `4c35b349`, سعر 679.99) مربوط بـ **8 بنود** من أنواع مختلفة بشكل خاطئ:

| البند | السعر الحالي (خاطئ) | السجل الصحيح | السعر الصحيح |
|---|---|---|---|
| كمرات ربط الأساسات | 679.99 | كمرات ربط الأساسات (27282146) | 1,350 |
| رقاب الأعمدة | 679.99 | يحتاج سجل مكتبة مخصص | — |
| بلاطة على الأرض | 679.99 | يحتاج سجل مكتبة مخصص | — |
| حوائط القص (صحيح) | 679.99 | حوائط القص (4c35b349) | 679.99 ✅ |
| السلالم | 679.99 | يحتاج سجل مكتبة مخصص | — |

## السبب الجذري
المحرك يطابق بناءً على الوصف الطويل المشترك (خرسانة مسلحة مصبوبة...) ويتجاهل اسم البند الفعلي بعد `—`. رغم إضافة `extractCleanSegment`، هذه البنود سُعّرت **قبل** الإصلاح ولم تُعاد مطابقتها.

## الخطة

### 1. إعادة مطابقة البنود الـ 8 المربوطة خطأ
- فك ربط البنود المرتبطة بـ `4c35b349` (ما عدا حوائط القص الفعلية)
- إعادة تشغيل محرك V3 عليها مع `extractCleanSegment` لتطابق السجلات الصحيحة
- البنود التي لها سجل مكتبة مخصص (مثل كمرات ربط) ستربط تلقائياً

### 2. فحص شامل للمطابقات المشابهة
- البحث عن سجلات مكتبة أخرى مربوطة بأكثر من 5 بنود من أنواع مختلفة (مؤشر على خطأ مطابقة جماعي)
- تقرير بالسجلات المشبوهة

### 3. إضافة حماية "Structural Type Gate" للأنواع الإنشائية
- في `matchingV3.ts`: إضافة تحقق أن البند والسجل من نفس النوع الإنشائي (كمرات ≠ حوائط قص ≠ أعمدة ≠ بلاطات ≠ سلالم)
- هذا يمنع تكرار المشكلة مستقبلاً

### الملفات المتأثرة

| الملف | التغيير |
|---|---|
| `src/lib/pricing/matchingV3.ts` | تعزيز Structural Type Gate |
| `src/lib/pricing/synonyms.ts` | أزواج تعارض إنشائية جديدة |
| `src/lib/pricingEngine.ts` | دالة إصلاح المطابقات الخاطئة |

