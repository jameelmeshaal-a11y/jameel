

# خطة التنفيذ النهائية — 4 خطوات

## الوضع الحالي
- المواسير 150مم: **مفكوكة فعلاً** (pending بدون ربط) — ستبقى كذلك لأن الانحراف بسبب اختلاف نطاق العمل وليس الوحدة
- الـ 38 بند المفكوكة: تم فكها بنجاح في الجولة السابقة
- البنود اليدوية (300): سليمة 100%

## الخطوات

### الخطوة 1: إعادة تسعير البنود الـ pending
- تشغيل محرك V3 على كل بند `status = 'pending' AND linked_rate_id IS NULL`
- المحرك سيعمل مع بوابات التعارض الجديدة (كمرات≠حفريات، مضخة≠رشاش، إلخ)
- البنود بدون سجل مكتبة مطابق (مواسير 150مم، نوافذ Ws04-08، WD01) ستبقى pending
- عرض تقرير نهائي: البند → السعر القديم → الجديد → السجل المربوط

### الخطوة 2: تقرير انحراف دوري
في `src/lib/pricing/integrityChecker.ts`:
- إضافة دالة `findDeviationItems()` تكشف البنود ذات الانحراف >30%
- تصنيف: ربط خاطئ / اختلاف مواصفات / يحتاج مراجعة
- عرض في `src/components/PricingIntegrityReport.tsx`

### الخطوة 3: حماية البنود اليدوية (تعزيز)
- في `integrityChecker.ts`: حارس صريح يرفض أي تغيير على بنود `override_type = 'manual'`
- في `BoQTable.tsx`: شارة "🔒 محمي يدوياً" بجانب البنود اليدوية

### الخطوة 4: اختبارات Regression
في `matchingV3.test.ts`:
- بلاط سيراميك ≠ إيبوكسي
- كمرات ≠ حفريات
- مضخة ≠ رشاش
- حماية البنود اليدوية من repricing

## الملفات المتأثرة
| الملف | التغيير |
|---|---|
| `src/lib/pricing/integrityChecker.ts` | تقرير انحراف + حارس manual |
| `src/components/PricingIntegrityReport.tsx` | عرض تقرير الانحراف |
| `src/components/BoQTable.tsx` | شارة محمي يدوياً + تشغيل إعادة التسعير |
| `src/lib/pricing/matchingV3.test.ts` | اختبارات regression |

