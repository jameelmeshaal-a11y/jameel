

# تشخيص وإصلاح مشكلة تسعير "كمرات ربط الأساسات" — تحليل جذري من البيانات الحية

## المشكلة المكتشفة بالتحقق من قاعدة البيانات

البند `7365213f` في ملف "حجز النساء عسير" (`05409cd3`):
- **item_no**: كمرات ربط الأساسات
- **description**: خرسانة مسلحة مصبوبة... وذلك لـ **— كمرات ربط الأساسات** ✅ (الاسم موجود في النهاية)
- **linked_rate_id**: `0e0b2e01` = **"خرسانة عادية - فرشات نظافة تحت الأساسات"** ❌ بسعر 188.4 ريال
- **السعر الحالي**: 188.4 ريال ❌ (الصحيح: 1,350 ريال)
- **confidence**: 52% فقط (needs_review)

**السجل الصحيح موجود في المكتبة**: `27282146` = "كمرات ربط الأساسات" بسعر 1,350 ريال ✅
وباقي بنود كمرات الربط في المشاريع الأخرى مربوطة بالسجل الصحيح!

## السبب الجذري الحقيقي (3 عوامل مجتمعة)

### 1. غياب أزواج تعارض بين "فرشات النظافة" والعناصر الإنشائية
```text
الموجود حالياً:
  خرسانة_نظافة ↔ خرسانة_مسلحة  ✅
  خرسانة_نظافة ↔ حديد_تسليح    ✅
  خرسانة_نظافة ↔ بلاط_ارضي     ✅

المفقود:
  خرسانة_نظافة ↔ كمرات_ربط      ❌ ← السبب المباشر
  خرسانة_نظافة ↔ كمرات           ❌
  خرسانة_نظافة ↔ حوائط_قص        ❌
  خرسانة_نظافة ↔ بلاطات          ❌
  خرسانة_نظافة ↔ اعمدة           ❌
  خرسانة_نظافة ↔ رقاب_اعمدة      ❌
  خرسانة_نظافة ↔ سلالم           ❌
  خرسانة_نظافة ↔ بلاطة_ارضية     ❌
  خرسانة_نظافة ↔ قواعد           ❌
```

### 2. Structural Type Gate لا يحظر "فرشات النظافة"
البوابة الإنشائية تحظر فقط عندما **كلا الطرفين** لهما نوع إنشائي. سجل "خرسانة عادية - فرشات نظافة" ليس له نوع إنشائي → يمر من البوابة بدون حظر.

### 3. `linked_rate_id` القديم الخاطئ يؤثر على النتيجة
عند ضغط المستخدم على ⟲ (إعادة تسعير فردي)، المحرك يمرر `linked_rate_id` الحالي (الخاطئ) كتلميح، فيحصل السجل الخاطئ على bonus +5/+10 نقاط.

## الحل — 4 تعديلات

### 1. `src/lib/pricing/synonyms.ts` — إضافة 9 أزواج تعارض مفقودة
إضافة تعارض بين "خرسانة_نظافة" وجميع العناصر الإنشائية:
```typescript
["خرسانة_نظافة", "كمرات_ربط"],
["خرسانة_نظافة", "كمرات"],
["خرسانة_نظافة", "حوائط_قص"],
["خرسانة_نظافة", "اعمدة"],
["خرسانة_نظافة", "رقاب_اعمدة"],
["خرسانة_نظافة", "بلاطات"],
["خرسانة_نظافة", "بلاطة_ارضية"],
["خرسانة_نظافة", "سلالم"],
["خرسانة_نظافة", "قواعد"],
```

### 2. `src/lib/pricing/matchingV3.ts` — تعزيز "Perfect Name Match"
إضافة مسار سريع: إذا كان `extractCleanSegment(description)` يطابق `standard_name_ar` لسجل مكتبة بشكل شبه كامل (تشابه ≥ 0.90)، يحصل على bonus +30 نقطة. هذا يضمن أن "كمرات ربط الأساسات" تطابق "كمرات ربط الأساسات" دائماً وبثقة عالية.

### 3. إصلاح البند المتضرر في قاعدة البيانات
تحديث البند `7365213f` لربطه بالسجل الصحيح `27282146` بسعر 1,350 ريال.

### 4. `src/lib/pricing/matchingV3.test.ts` — اختبار تراجعي
إضافة اختبار يؤكد أن "كمرات ربط الأساسات" لا تطابق "خرسانة عادية - فرشات نظافة" ويطابق السجل الصحيح.

## فحص شامل إضافي
بعد الإصلاح: فحص جميع البنود المربوطة بسجل "فرشات النظافة" (`0e0b2e01`) للتأكد أن لا يوجد بنود إنشائية أخرى مربوطة خطأ.

## الملفات المتأثرة

| الملف | التغيير |
|---|---|
| `src/lib/pricing/synonyms.ts` | 9 أزواج تعارض جديدة |
| `src/lib/pricing/matchingV3.ts` | Perfect Name Match bonus +30 |
| `src/lib/pricing/matchingV3.test.ts` | اختبار تراجعي |
| قاعدة البيانات | إصلاح البند `7365213f` |

